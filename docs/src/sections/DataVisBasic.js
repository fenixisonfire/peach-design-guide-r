import React from 'react';

import Anchor from '../Anchor';
import PropTable from '../PropTable';
import ReactPlayground from '../ReactPlayground';
import Samples from '../Samples';
import ReactDOM from 'react-dom'
import ReactSVG from 'react-svg'
import Image from '../../../src/Image';

export default function DataVisBasic() {
    return (
        <div className="bs-docs-section">
            <h2 className="page-header">
                <Anchor id="datavisbasic">Basic Graphs</Anchor>
            </h2>
            <p>
                This section outlines the guidelines for <b>basic</b> graphs in the Peach Project.

                <br></br>
                <br></br>

                The majority of the data visualisation needs for PEACH will be met by one of the following basic graph types. These simple non interactive graphs are easy to create and can be readily embedded in PEACH sites.



            </p>
        </div>
    );
}
